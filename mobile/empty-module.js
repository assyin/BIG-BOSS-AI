// Empty stub module used by metro.config.js to alias optional @mediapipe/*
// imports inside @tensorflow-models/pose-detection. We only use MoveNet
// (not BlazePose), so MediaPipe runtime is not needed on React Native.
module.exports = {};

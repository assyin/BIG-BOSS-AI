# .NET
bin/
obj/
*.user
*.suo
*.userprefs
*.sln.docstates
appsettings.Development.json
appsettings.*.json
!appsettings.json

# Node
node_modules/
.expo/
dist/
.next/
out/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Credentials files (API keys etc — NEVER commit)
credentiel.txt
credentials.txt
*.credentials
secrets.txt

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
build/
*.ipa
*.apk
*.aab

# Testing
coverage/
*.lcov

# Misc
*.pem
*.p12
.cache/

# Videos (served locally, too large for git)
videos/
temp-expo/
influencer-videos/*.mp4
influencer-videos/*.mov

# Audio cache (TTS responses, regenerated on demand)
backend/audio-cache/

# Influencer audio - generated/intermediate files (only raw/ is tracked)
influencer-audio/*.mp3
influencer-audio/vocals/
influencer-audio/filelist.txt

# Temp/intermediate data
gemini_input_list.txt

# Windows NTFS alternate data streams (created when files are downloaded from internet)
*:Zone.Identifier

# Local screenshots accidentally dropped at REPO ROOT (not assets in subdirs)
/*.jpeg
/*.jpg
/*.png

# WhatsApp shared images dropped in repo
WhatsApp Image*

# IDE / Editor metadata
.claude/

# Local working folders (not for repo)
Tanslation Darija Files/
tests/qr/
mobile/tests/e2e-*-result.json
